Needed feature: Character UI image, Initial messages (first messages) downloaded with soul.md, maybe a feature of chaneging narrator's input into a gif/image/or manga
add multi character feature so that you can add several soul to one setting and single chat.
maybe feed AI and make it respond per soul? So that each character responds in sperate messages?
Deleting chat need to unwind soul somehow... Logs? Also, need to allow regenerate response or ask ai to fix last response with directions. 
also need to enable moving back and forth between generated messeages to let them decide which message they want to use.
get rid of mock or make API default option. I don't have an AI plan like JLLM that people pay yet so rn API should be default not a mock.